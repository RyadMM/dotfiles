# =============================================================================
# Main Zsh Configuration
# =============================================================================
# Modular configuration is loaded from ~/.config/zsh/
#
# To customize:
#   1. Edit individual modules in ~/.config/zsh/
#   2. Or create ~/.config/zsh/local.zsh for personal overrides (gitignored)
#
# QUICK RELOAD: Run `r` to reload shell configuration
# =============================================================================
[ -f ~/.config/zsh/init.zsh ] && source ~/.config/zsh/init.zsh
